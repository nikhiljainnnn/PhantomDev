---
# Celery Worker Deployment
# Workers pick up pipeline tasks from Redis queue
# Scale this up when you have many concurrent pipeline requests
apiVersion: apps/v1
kind: Deployment
metadata:
  name: phantomdev-worker
  namespace: phantomdev
  labels:
    app: phantomdev-worker
spec:
  replicas: 2          # Start with 2 workers = 2 concurrent pipelines
  selector:
    matchLabels:
      app: phantomdev-worker
  template:
    metadata:
      labels:
        app: phantomdev-worker
    spec:
      containers:
        - name: worker
          image: phantomdev:latest
          imagePullPolicy: IfNotPresent
          command:
            - celery
            - -A
            - worker.celery_app
            - worker
            - --loglevel=info
            - --concurrency=1   # 1 pipeline per worker pod (LLM is the bottleneck)
            - -Q
            - pipeline
          envFrom:
            - configMapRef:
                name: phantomdev-config
            - secretRef:
                name: phantomdev-secrets
          resources:
            requests:
              memory: "512Mi"
              cpu: "250m"
            limits:
              memory: "2Gi"      # Pipelines can use more memory
              cpu: "1000m"
          volumeMounts:
            - name: workspace
              mountPath: /app/workspace
            - name: chroma
              mountPath: /app/data/chroma
          livenessProbe:
            exec:
              command:
                - celery
                - -A
                - worker.celery_app
                - inspect
                - ping
            initialDelaySeconds: 30
            periodSeconds: 60
            failureThreshold: 3
      volumes:
        - name: workspace
          persistentVolumeClaim:
            claimName: workspace-pvc
        - name: chroma
          persistentVolumeClaim:
            claimName: chroma-pvc
